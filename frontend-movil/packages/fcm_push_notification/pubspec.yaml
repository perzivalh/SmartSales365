name: fcm_push_notification
description: Stub package to satisfy Firebase Messaging optional injector.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
